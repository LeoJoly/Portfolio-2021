import React from 'react';

// comptonent
const Header = () => (
  <header className="header row">
    <nav className="menu row">
      <a className="menu-btn" href="/">About</a>
      <a className="menu-btn" href="/">Work</a>
      <a className="menu-btn" href="/">Contact</a>
    </nav>
  </header>
);

export default Header;